name: Deploy Prioritized Feature

on:
  push:
    branches:
      - 'feature/**'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install & Build
        run: npm install && npm run build
      - name: Deploy to Staging
        run: npm run deploy:staging
